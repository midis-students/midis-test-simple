import { Question, tools } from '.';
const q: Question = {
  short: 'QTitle',
  query() {
    return ``;
  },
  answer(container, qId) {
    container.innerHTML = ``;
  },
};

export default q;
